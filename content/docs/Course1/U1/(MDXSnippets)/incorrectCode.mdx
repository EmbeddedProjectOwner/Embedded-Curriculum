---
title: IncorrectSnippets
full: false
---
import { CodeBlock, Pre, } from "fumadocs-ui/components/codeblock"
import { IoLogoHtml5 } from "react-icons/io5"

<CodeBlock icon={<IoLogoHtml5 />} allowCopy={false}>
    <Pre>
        <>
            ```html title="index.html"
            <p title='my text'></p> <!-- [!code ++] -->

            <!-- But what if I want to add emphasis? -->
            <p title='my 'amazing' text'></p> // [!code --]
            <!-- It wouldnt work because we escape the character with another ' -->
            ```
    </>
</Pre>
</CodeBlock >

<CodeBlock icon={<IoLogoHtml5 />} allowCopy={false}>
    <Pre>
            <>
                ```html
                <!-- SO we have to instead use on another character, we can do this two ways: -->
                <p title="my 'amazing' text"></p> <!-- [!code ++] -->
                <!-- Or we can use `, which will allow both ", and ' -->
                <!-- `, *in* HTML requires that you wrap a backtick with { } -->

                ```
    </>
</Pre>
</CodeBlock >

